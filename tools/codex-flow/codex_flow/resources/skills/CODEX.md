# Scope

Codex is running through `codex-flow` for external-input implementation or review.

Supported implementation languages:
- C++
- Python
- Bash

Hard safety guarantees come from the runner. The model must still respect the contract, but must not
assume prompt text replaces enforcement.
