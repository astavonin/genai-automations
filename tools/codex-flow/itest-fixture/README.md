# codex-flow smoke repo

Small standalone repository fixture for testing `codex-flow implement`.

Expected experiment:
- start from the current simple implementation in `src/example.py`
- use the sample implementation request from `../examples/`
- let `codex-flow` add the greeting helper and update tests
